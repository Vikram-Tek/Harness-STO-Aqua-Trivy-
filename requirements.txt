aiohttp-jinja2==1.1.0
aiohttp-session==2.7.0
aiohttp==3.5.3
aiopg==0.15.0
aioredis==1.2.0
async-timeout==3.0.1      # via aiohttp, aioredis
attrs==18.2.0             # via aiohttp
chardet==3.0.4            # via aiohttp
hiredis==0.3.1            # via aioredis
idna==2.8                 # via yarl
jinja2==2.10              # via aiohttp-jinja2
markupsafe==1.1.0         # via jinja2
multidict==4.5.2          # via aiohttp, yarl
psycopg2==2.7.6.1         # via aiopg
pyyaml==3.13
trafaret-config==2.0.2
trafaret==1.2.0
yarl==1.3.0               # via aiohttp
# Web frameworks with known CVEs
Django==2.2.0          # Multiple CVEs (SQL injection, XSS, etc.)
Flask==0.12.2         # Old version, multiple security issues

# HTTP / networking
requests==2.19.1      # CVE-2018-18074
urllib3==1.24.1       # Multiple CVEs

# Serialization / parsing
PyYAML==3.13          # Unsafe load vulnerability (high severity)
jsonschema==2.6.0

# Crypto / auth
cryptography==2.3     # Multiple CVEs
pyjwt==1.7.1          # Algorithm confusion issues

# Template / utils
Jinja2==2.10          # Sandbox escape CVEs
MarkupSafe==1.0

# ML / Data (as requested)
lightgbm==2.2.3       # Old version – has dependency & build related CVEs
numpy==1.16.0        # Multiple memory / overflow CVEs
pandas==0.24.2

# Others with common CVEs
Pillow==6.0.0
lxml==4.2.1
 


